SECRET_KEY=MySuperSecretKey123!

# Database configuration (adjust as needed)
DB_HOST=host.docker.internal   # Use 'mysql' if using Docker Compose with a MySQL service, or 'localhost' for local dev
DB_USER=root
DB_PASSWORD=                   # Fill in if your MySQL has a password
DB_NAME=meeting_room_booking
DB_PORT=3306
